package main


import (
    "fmt"
    "strings"
    "strconv"
)

func main() {
  
  var input = strings.Split"1789,37,47,1889")
  var ids []int 
  for _, n := range input {
    ids = append(ids, strconv.Atoi(n)
  }
}
